project_name: unified-memory-system
created: 2026-06-15
mode: [library, cli]
scope_note: "통합 기억 시스템 — post-it(단기)·auto-memory(장기)·user_profile(전역) 1 store + tier 모델. Hermes 적극 결합 + 중복 cut."
phases:
  spec: done               # v3 PRD 확정 (D-1~7 lock — DB화·저장소분리·통합강화)
  scaffolding: pending
  dev: in_progress         # v3 완료(DB화·분리). v4 신규 = Cluster A(profile 완전통합)+B(Hermes port) 구현 대기
last_updated: 2026-06-16  # v8: D-14 권한 allowlist 무력 실측 → no-tools+스크립트 실행 재설계. snapshot v7 (v6 e491241·v7 fab5b46 머지됨)
inputs:
  - .claude_reports/research/hermes-agent/
  - VISION.md
  - CONVENTIONS.md#7
  - skills/post-it/ + tools/memory/ + user_profile/
decisions_locked:
  - D-1: 삭제 정책 — working 자동만료/졸업, durable gc 플래깅
  - D-2: post-it 스킬 — mem alias 유지 (deprecate 아님)
  - D-3: user_profile — raw=레코드, aspect=DB→generated view
  - D-4: 주입·회수 — SessionStart inject + SessionEnd sync
  - D-5: SoT = 로컬 SQLite memory.db (git=dump.jsonl 텍스트 mirror, .db 비추적)
  - D-6: 저장소 분리 — claude-memory private repo, config repo memory/ 이력 filter-repo 제거
  - D-7: 통합 깊이 — user_profile+post-it+auto-memory 한 DB (tier/scope/type 컬럼 구분 유지)
  - D-8: 결정론 우선 (deterministic-first) — SW 가능 요소는 코드(hook/script/gate/DB)로, agent 판단 최소화 (cross-cutting, DESIGN_PRINCIPLES 격상)
  - D-9: 파일 메커니즘 제거(Option 2) — user_profile/·post-it.md 파일 제거, DB 유일 SoT, sub-agent는 mem profile/recall로 DB 직접 읽기 (Cluster A)
  - D-10: Hermes 잔여 port — session_search 자율호출 + turn-counter 자기회고, 둘 다 결정론-우선 (Cluster B)
  - D-11: harness-agnostic 세션 source 추상화 — ingest_session(source), 현재 Claude Code jsonl adapter 1개, 미래 하네스는 adapter 추가 (Cluster C)
  - D-12: SessionEnd distiller — detached claude -p로 세션 jsonl → working/durable distill, 재귀가드 MEM_DISTILL=1, 증분 marker (Cluster C)
  - D-13: in-session 증분 consolidation — turn-nudge 확장, delta만 정리(요약 추가/해결분 prune). v7: 메인 아니라 외부 detached distiller 분사(메인 load 0), D-12와 같은 worker·marker (Cluster C)
  - D-14: distiller 신뢰경계 차단 — v7 권한 allowlist(mem.py-only) 무력 실측(settings.json blanket Bash allow) → v8 재설계: distiller no-tools(--disallowedTools) + JSON-lines 출력 → 스크립트가 검증 후 mem add 직접 실행(LLM 판단·코드 실행, §0.5). distiller 모델 sonnet (Cluster C v8)
